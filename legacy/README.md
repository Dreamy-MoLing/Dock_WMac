# Legacy Archive

The Qt v1 source tree has been removed from the active repository context.

Active development is under `src/v2/` and uses C++20, C++/WinRT, WinUI 3, and
Windows App SDK through `Dock_WMac_v2.sln`.

The former Qt archive is recoverable from Git history. A local non-active copy
may also exist outside this workspace at `..\Dock_WMac.qt-v1-local-archive`.
