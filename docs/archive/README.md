# Archive

This directory is Windows-only project history. Files here are not v2
requirements and must not be used as active architecture, product, or build
guidance.

Use the current v2 docs in `docs/` and `AGENTS.md` instead. Archived v1 plans may
describe old toolchains, old folder layouts, or rejected platform ideas.
