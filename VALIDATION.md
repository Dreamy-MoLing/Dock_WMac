# Dock_WMac 需求验证与发布验收

本项目当前定位为“原生、便携、低干扰的 Windows 应用 Dock”，不是完整任务栏或
Windows Shell 替代品。验证期不增加开始菜单、通知区域、组件市场或大型主题系统。

## 14 天需求验证

1. 发布一个仅包含核心 Dock、窗口切换、预览、固定项和自动隐藏的候选版本。
2. 招募至少 10 名非作者用户。安装、D1、D7 和卸载反馈全部由用户自愿填写；程序不上传遥测。
3. 在下表逐人记录，不填写用户名、机器名、程序列表或其他不必要的隐私信息。

| 匿名编号 | 安装成功 | D1 使用 | D7 使用 | 配合/隐藏任务栏 | 首要故障 | 卸载原因 | 优先于现有方案 | 愿意推荐/付费 |
|---|---|---|---|---|---|---|---|---|
| U01 | | | | | | | | |

决策规则：

- 继续：至少 10 个有效安装、5 个 D7 用户、3 个用户明确优先选择本项目。
- 转向：安装数达标但 D7 少于 5；只处理多名用户重复指出的流失原因。
- 归档：14 天有效安装少于 5，或没有用户愿意持续使用。

## Windows 发布验收

每个候选 ZIP 在未安装 Qt 的干净 Windows 10 和 Windows 11 环境分别验证：

- 解压并启动 `WMacDock.exe`；首次启动原生任务栏保持可见。
- 启用/关闭实验性隐藏选项；正常退出、强制结束后重新启动均可恢复任务栏。
- 开机自启开关可逆；移动整个程序目录后注册表命令更新到新路径。
- 普通可写目录使用旁置 `data/`；只读目录回退到 `%LOCALAPPDATA%\Dock_WMac\data`。
- 100%、125%、150%、200% DPI，以及单屏、主副屏、热插拔场景定位正确。
- Explorer 重启后 Dock 可用；任务栏不会因为 Dock 未显示或启动失败而被隐藏。
- Win32、UWP、多进程应用、最小化窗口和同应用多个窗口可启动、切换并预览。
- ZIP 含主程序、Qt6 DLL、MSVC 运行库、`platforms/` 和所需样式插件。

## 空闲资源记录

候选版本启动后静置 10 分钟，分别记录原生任务栏可见和实验性隐藏两种状态：

| 环境 | 平均 CPU | 峰值 CPU | 工作集 | 私有字节 | 10 分钟异常日志数 |
|---|---:|---:|---:|---:|---:|
| 任务栏可见 | | | | | |
| 任务栏隐藏 | | | | | |

使用任务管理器或 Windows Performance Recorder 采样。重点核对底边检测 200ms、进程扫描
2s、主题检测 5s、窗口数量兜底 10s 的唤醒来源。未完成测量前不对外宣称“低资源占用”。

## 发布门禁

- PR、master push 和 `v*` tag 均通过 configure、Release build、ctest、CPack。
- `test_windows_integration` 在真实 Qt 窗口上通过 DWM 边界、cloaked、display affinity 和 DPI 检查。
- 下载 artifact 并人工检查 ZIP 内容，再决定是否公开创建 Release。
