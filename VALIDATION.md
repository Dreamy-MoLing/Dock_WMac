# Validation

The active Windows-only v2 validation plan lives in `docs/VALIDATION.md`.

This root file is kept only as a stable pointer for older links. The previous
Qt validation checklist is v1 history and is no longer the current release gate.
